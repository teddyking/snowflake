---

jobs:
- name: unit
  plan:
  - get: snowflake
    trigger: true
  - task: unit
    file: snowflake/build/ci/unit.yml

resources:
- name: snowflake
  type: git
  source:
    uri: https://github.com/teddyking/snowflake
    branch: master

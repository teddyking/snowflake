syntax = "proto3";

import "types.proto";

package api;

service Flaker {
  rpc List (FlakeListRequest) returns (FlakeListResponse) {}
}

message FlakeListRequest {
  repeated SuiteSummary suites = 1;
}
message FlakeListResponse {
  repeated Flake flakes = 1;
}
